import 'package:flutter/material.dart';

class LogueadoPage extends StatelessWidget {
  const LogueadoPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Logueado page lol"),
      ),
      body: Center(child: Text("Logeado")),
    );
  }
}
